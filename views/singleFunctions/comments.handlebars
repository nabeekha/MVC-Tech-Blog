<div class="comments">
    {{#each this}}
    <section>
    <div class="meta">
        {{user.username}} on {{created_at}}
    </div>
    <div class="words">
        {{comment_text}}
    </div>
    </section>
    {{/each}}
</div>